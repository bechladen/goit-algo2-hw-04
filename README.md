## goit-algo2-hw-04 — 2 задачі

Вимоги: **Python 3.10+**.

### Задача 1 — Максимальний потік (Едмондс–Карп)

- **Файли**:
  - `task_1.py` — розрахунок максимального потоку + покрокові шляхи + таблиця “Термінал→Магазин”
  - `REPORT.md` — **звіт саме для Задачі 1**

- **Запуск**:

```bash
python3 task_1.py
```

- **Очікувано**: друк максимального потоку (**115**), кроків алгоритму та підсумкової таблиці.

### Задача 2 — Розширення префіксного дерева (Trie)

- **Файли**:
  - `trie.py` — базовий клас `Trie` (як у шаблоні `from trie import Trie`)
  - `task_2.py` — клас `Homework(Trie)` + методи `count_words_with_suffix`, `has_prefix`

```bash
python3 task_2.py
```

- **Очікувано**: якщо всі вбудовані `assert` пройшли — виведе **`OK`**.

